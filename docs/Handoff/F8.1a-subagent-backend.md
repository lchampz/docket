# F8.1a · subagent — Backend: domínio de volta ao português

Projeto: `~/Documents/dev/docket/backend`.

A fase anterior traduziu tudo para inglês. **Isso foi longe demais.** Agora: substantivo do negócio em português, tudo o mais em inglês.

## A regra

| Categoria | Idioma |
|---|---|
| Entidade, campo, enum, rota, código de erro, pacote | **pt-BR** |
| Verbo de método, preposição, sufixo estrutural, termo técnico | **inglês** |
| Mensagem de validação e texto de tela | **pt-BR** (já estão) |
| Comentário e Javadoc | **pt-BR** |

## Renomes — inglês → português

| agora | volta a ser |
|---|---|
| pacote `offices` | `cartorios` |
| pacote `documents` | `documentos` |
| pacote `requests` | `agendamentos` |
| pacote `orders` | `pedidos` |
| `NotaryOffice` | `Cartorio` |
| `DocumentType` | `Documento` |
| `Order`, `OrderStatus` | `Pedido`, `StatusPedido` |
| `DocumentRequest`, `RequestStatus` | `Agendamento`, `StatusAgendamento` |
| `PersonType` (`INDIVIDUAL`/`COMPANY`) | `TipoPessoa` (`FISICA`/`JURIDICA`) |
| `OrderStatus` valores | `EM_ANDAMENTO`, `CONCLUIDO`, `CANCELADO` |
| `RequestStatus` valores | `PENDENTE`, `EM_ANDAMENTO`, `CONCLUIDO`, `CANCELADO` |
| `name`, `zipCode`, `street`, `number`, `district`, `city`, `state` | `nome`, `cep`, `rua`, `numero`, `bairro`, `cidade`, `uf` |
| `legalName`, `taxId`, `birthDate` | `nomeRazaoSocial`, `documentoIdentificacao`, `dataNascimento` |
| `notes`, `createdBy` | `observacao`, `criadoPor` |
| `documentTypeIds`, `totalDocumentTypes`, `totalRequests` | `documentoIds`, `totalDocumentos`, `totalAgendamentos` |
| `notaryOfficeId`, `documentTypeId` | `cartorioId`, `documentoId` |
| `documentTypeName`, `notaryOfficeName` | `documentoNome`, `cartorioNome` |
| `TaxIds` | `Documentos` |
| `ConsistentTaxId`, `HasTaxId` | `IdentificacaoCoerente`, `PossuiIdentificacao` |
| `NotaryOfficeForm` | `CartorioForm` |

**Códigos de erro voltam ao português:** `VALIDATION`→`VALIDACAO`, `NOT_FOUND`→`NAO_ENCONTRADO`, `INVALID_OFFICE_DOCUMENT_PAIR`→`PAR_CARTORIO_DOCUMENTO_INVALIDO`, `DUPLICATE_NAME`→`NOME_DUPLICADO`, `RESOURCE_IN_USE`→`RECURSO_EM_USO`, `INTERNAL_ERROR`→`ERRO_INTERNO`.

**Rotas voltam ao português:**
```
/api/v1/cartorios[/{id}]              ?page&size&nome
/api/v1/cartorios/{id}/documentos
/api/v1/documentos[/{id}]             ?page&size
/api/v1/pedidos[/{id}]
/api/v1/pedidos/{id}/agendamentos     GET e POST
/api/v1/agendamentos/{id}             GET e DELETE
/api/v1/agendamentos/{id}/status      PATCH
```
Thymeleaf volta para `/cartorios`; `/` redireciona para lá.

## Ficam em INGLÊS

Classes de apoio e infraestrutura mantêm o nome em inglês herdado da F8:

`ErrorHandler`, `ErrorCode`, `ConflictException`, `ResourceNotFoundException`, `FieldError`, `PageResponse`.

**Métodos passam a ter verbo em inglês sobre substantivo português:**

| antes (pt) | agora |
|---|---|
| `listar(...)` | `list(...)` |
| `buscarPorId(...)` | `findById(...)` |
| `criar(...)` | `create(...)` |
| `atualizar(...)` | `update(...)` |
| `excluir(...)` | `delete(...)` |
| `listarDocumentos(...)` | `listDocumentos(...)` |
| mapper `paraResponse` | `toResponse(...)` |
| mapper `paraEntidade` | `toEntity(...)` |
| `CartorioForm.paraRequest()` | `CartorioForm.toRequest()` |
| `Documentos.normalizarIdentificacao` | `Documentos.normalize` |
| `Documentos.somenteDigitos` | `Documentos.onlyDigits` |
| `Documentos.cpfValido` / `cnpjValido` | `Documentos.isCpfValid` / `isCnpjValid` |

Nomes de variável local e parâmetro: substantivo do domínio em português (`cartorio`, `documento`, `agendamento`), o resto em inglês.

## Banco

- **Apagar `V5__enums_em_ingles.sql`.** Nada foi liberado; o schema volta a ser exatamente V1–V4
- `R__seed.sql` volta a `'EM_ANDAMENTO'` no status do pedido
- Nomes de tabela e coluna **nunca mudaram** — continuam em português
- Com campo e coluna voltando a coincidir, **remover os `@Column(name=...)` que viraram redundantes**. Manter só onde o nome difere de verdade (ex.: `nome_razao_social` ↔ `nomeRazaoSocial` continua precisando, porque a coluna é snake_case)

## Verificação

```bash
cd ~/Documents/dev/docket && docker compose down -v && docker compose up -d --build
```

- [ ] `ls src/main/resources/db/migration/` → **sem `V5`**
- [ ] Backend `healthy` com `ddl-auto: validate`
- [ ] `GET /api/v1/documentos` → 6 itens, `size` 10
- [ ] `GET /api/v1/cartorios` → `totalDocumentos` 6 / 3 / 3 / 2
- [ ] `GET /api/v1/cartorios/4/documentos` → Escritura Pública e Procuração
- [ ] POST `/pedidos/1/agendamentos` com `cartorioId=4, documentoId=1` → `409 PAR_CARTORIO_DOCUMENTO_INVALIDO`
- [ ] POST com `tipoPessoa=FISICA`, `documentoIdentificacao="111.444.777-35"` → `201`, `status` `PENDENTE`
- [ ] `"111.444.777-34"` → `400`, `errors[0].campo == "documentoIdentificacao"`, mensagem em português
- [ ] `POST /documentos` com `"certidão de nascimento"` → `409 NOME_DUPLICADO`
- [ ] `POST /cartorios` com `{}` → `400` com 7 mensagens em português
- [ ] `localhost:8080/cartorios` (Thymeleaf) funciona, tela em português
- [ ] `grep -rnE "public .*(listar|buscarPorId|criar|atualizar|excluir)\(" src/main/java` → **vazio** (verbos em inglês)
- [ ] `grep -rn "NotaryOffice\|DocumentType\|DocumentRequest\|taxId\|legalName" src/main/java` → **vazio**

**Limpe o que criar nos testes.** O front vai ficar quebrado — a próxima etapa cuida.
